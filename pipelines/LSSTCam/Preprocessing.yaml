description: Preprocessing pipeline specialized for LSSTCam

imports:
  - location: $AP_PIPE_DIR/pipelines/LSSTCam/ApPipe.yaml
    include:
      - preload
parameters:
  release_id: 1  # This pipeline runs in production (parameter should be unused in preprocessing)
