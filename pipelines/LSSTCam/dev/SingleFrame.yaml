description: >-
  Alert Production pipeline specialized for LSSTCam.
  This version is specialized for the dev environment.

imports:
  - location: $PROMPT_PROCESSING_DIR/pipelines/LSSTCam/ApPipe.yaml
parameters:
  release_id: 0  # This pipeline runs in dev
