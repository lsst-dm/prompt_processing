description: >-
  ISR-only pipeline specialized for LSSTCam.
  This version is specialized for the dev environment.

imports:
  - location: $PROMPT_PROCESSING_DIR/pipelines/LSSTCam/Isr.yaml
parameters:
  release_id: 0  # This pipeline runs in dev (parameter should be unused in ISR)
