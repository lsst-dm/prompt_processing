description: Preprocessing pipeline specialized for LATISS

imports:
  - location: $AP_PIPE_DIR/pipelines/LATISS/ApPipe.yaml
    include:
      - preload
parameters:
  release_id: 0  # We only run LATISS pipelines for testing purposes
